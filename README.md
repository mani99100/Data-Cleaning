# Data-Cleaning
Sorting by admission date ensures logical sequence in patient records. Forward fill is preferred as it extends the last valid entry to maintain continuity. Backward fill works when missing values align better with the next entry. Overall, forward fill is more reliable for medical datasets with sequential patient histories.
